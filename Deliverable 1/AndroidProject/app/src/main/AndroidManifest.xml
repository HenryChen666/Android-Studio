<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deliverable1_group7">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".update_delete_popup"></activity>
        <activity android:name=".layout_product_list" />
        <activity android:name=".service_creation" />
        <activity android:name=".appointment_creation" />
        <activity android:name=".AdminActivity"></activity>
        <activity android:name=".ClinicServices"></activity>
        <activity android:name=".AccountCreationScreen" />
        <activity android:name=".WelcomeScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
